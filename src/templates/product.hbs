<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=1080, height=1080" />
  <link rel="stylesheet" href="http://localhost:3000/styles/product.css" />
</head>

<body>

  {{!--
    TEMPLATE: product — launches, feature drops, pricing posts

    Required:  headline (product name)
    Optional:  subtitle (tagline), badge{text,type},
               features[{title,description}]
               price{amount,period}
               stats[{value,label}]
               quote{text,author}, cta{title,url}, tags[], footer
               background, layout (showcase|spotlight), accentColor
  --}}

  <div
    class="poster"
    data-layout="{{#if layout}}{{layout}}{{else}}showcase{{/if}}"
    style="background-image:url('http://localhost:3000/assets/backgrounds/{{background}}'); --accent: {{#if accentColor}}{{accentColor}}{{else}}#5B8DEF{{/if}};"
  >
    <div class="overlay">
      <span class="trace trace-tl" aria-hidden="true"></span>
      <span class="trace trace-br" aria-hidden="true"></span>

      <header class="header">
        <img class="logo logo-left" src="http://localhost:3000/assets/logos/silicofeller.png" alt="Silicofeller" />
        <img class="logo logo-right" src="http://localhost:3000/assets/logos/nvidia.png" alt="NVIDIA" />
      </header>

      {{#if badge}}
        <div class="badge-row">
          <span class="badge" data-type="{{#if badge.type}}{{badge.type}}{{else}}primary{{/if}}">{{badge.text}}</span>
        </div>
      {{/if}}

      <main class="content">

        <div class="text-block">
          <h1 class="headline">{{headline}}</h1>
          {{#if subtitle}}<h2 class="subtitle">{{subtitle}}</h2>{{/if}}
          <div class="rule"><span class="rule-node"></span></div>
        </div>

        {{#if price}}
          <div class="price-row">
            <span class="price-amount">{{price.amount}}</span>
            {{#if price.period}}<span class="price-period">/ {{price.period}}</span>{{/if}}
          </div>
        {{/if}}

        {{#if features}}
          <div class="features">
            {{#each features}}
              <div class="feature">
                <span class="feature-mark"></span>
                <div class="feature-body">
                  <div class="feature-title">{{title}}</div>
                  {{#if description}}<div class="feature-desc">{{description}}</div>{{/if}}
                </div>
              </div>
            {{/each}}
          </div>
        {{/if}}

        {{#if stats}}
          <div class="stats">
            {{#each stats}}
              <div class="card">
                <div class="number">{{value}}</div>
                <div class="label">{{label}}</div>
              </div>
            {{/each}}
          </div>
        {{/if}}

        {{#if quote}}
          <div class="quote">
            <p class="quote-text">{{quote.text}}</p>
            {{#if quote.author}}<div class="quote-author">{{quote.author}}</div>{{/if}}
          </div>
        {{/if}}

        {{#if cta}}
          <a class="cta">{{cta.title}}</a>
        {{/if}}

      </main>

      <footer class="footer">
        <span class="footer-line"></span>
        <div class="tags-row">
          {{#each tags}}
            <span class="tag">{{this}}</span>
          {{else}}
            <span class="tag">#SilicoFeller</span>
          {{/each}}
        </div>
        <div class="footer-brand">
          <span>{{#if footer}}{{footer}}{{else}}SilicoFeller • Engineering the Future of Chips{{/if}}</span>
        </div>
      </footer>

    </div>
  </div>

</body>

</html>
